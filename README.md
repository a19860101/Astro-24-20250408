# git

```bash
git config --global user.name 你的名字
git config --global user.email 你的email你的email

#查看設定
git config --list

#git初始化
git init

# 加入追蹤
git add .

# 提交到儲存庫
git commit -m '訊息'

# 硬回復
git reset 版本號 --hard

# 查詢紀錄
git log

git reflog
```